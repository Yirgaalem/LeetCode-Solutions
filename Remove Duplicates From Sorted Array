Authour: Nahor Yirgaalem
Date: July 13th, 2022
Time complexity: O(n)
Space complexity: O(1)

class Solution {
    public int removeDuplicates(int[] nums) {
        int count = 1; //Set to 1 because we know that nums[0] is a unique to the left of the array (since there is no left). We will then start our loop at nums[1]

        //Loops from nums[1] until nums[-1].
        for(int i = 1; i < nums.length; i++){
            //Checks if the current number is unique to the left fo the array (first of its value)
            if(nums[i] != nums[i-1]){
                //If it is then we set the number at index count equal to the current number, because count is the number of time we have seen a new number thus, it will automatically go into that position.
                nums[count] = nums[i];
                count++;
            }
         
        }
        
        return count; 
       
    }
} 
