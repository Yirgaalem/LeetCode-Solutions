Authour: Nahor Yirgaalem
Date: July 17th, 2022
Time complexity: O(n^2)
Space complexity: O(1)
/**
An algorithm that finds the first non repeating character in a string. String can be of anysize, only lowercase letters allowed
Example: aaabcccdeeef, first non repeating character is b, thus it will return b
*/

// Online Java Compiler
// Use this editor to write, compile and run your Java code online

class HelloWorld {
    public static void main(String[] args) {
        
        System.out.println(firstNonRepeatingCharacter("abcbad"));
        
    }
    public static char firstNonRepeatingCharacter(String str){
        
        
        for(int i = 0; i < str.length();i++){
        
            if(i > 1 && str.charAt(i) != str.charAt(i-1)){
            
                for(int j = i+1; j < str.length();j++){
                    if(j < str.length()-1){
                        if(str.charAt(i) == str.charAt(j))
                            break;
                    }
                    else{
                        if(str.charAt(i) == str.charAt(j))
                            break;
                        else
                            return str.charAt(i);
                    }
                }
            }
        }
        return '?';
    }
}
